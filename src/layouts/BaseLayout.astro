---
import logo from "../assets/logo.svg";
const {title} = Astro.props;
import "../styles/global.css";

const navLinks = [
  {href: "/", label: "Home"},
  {href: "/about", label: "About"},
  {href: "/services", label: "Services"},
  {href: "/projects", label: "Projects"},
  {href: "/contact", label: "Contact"},
];

const normalizePath = (path: string) => path.replace(/\/+$/, "") || "/";
const currentPath = normalizePath(Astro.url.pathname);
const isActive = (href: string) => {
  const target = normalizePath(href);
  if (target === "/") {
    return currentPath === "/";
  }
  return currentPath === target || currentPath.startsWith(`${target}/`);
};

---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
    <title>{title}</title>
	</head>

	<body class="bg-neutral-light text-neutral-dark font-sans">
    <nav class="sticky top-0 z-50">
      <div class="relative bg-neutral-dark/90 backdrop-blur supports-[backdrop-filter]:bg-neutral-dark/75 border-b border-white/10">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between gap-6">
          <a href="/" class="flex items-center gap-3 text-neutral-white">
            <img src={logo.src} alt="Luna Handyman Solutions logo" class="h-11 w-11 rounded-xl shadow-lg" />
            <div class="leading-tight">
              <p class="text-lg font-bold">Luna Handyman</p>
              <p class="text-sm text-neutral-light-gray">Builds • Renovations • Care</p>
            </div>
          </a>

          <div class="hidden md:flex items-center gap-8 text-neutral-light-gray font-medium">
            {navLinks.map((link) => (
              <a
                href={link.href}
                aria-current={isActive(link.href) ? "page" : undefined}
                class={`hover:text-accent transition ${isActive(link.href) ? "text-accent font-semibold" : ""}`}
              >
                {link.label}
              </a>
            ))}
          </div>

          <div class="hidden md:flex items-center gap-3">
            <a href="tel:+14157678986" class="inline-flex items-center gap-2 text-neutral-light-gray hover:text-accent transition">
              <span class="text-lg">📞</span>
              <span class="font-semibold">415-767-8986</span>
            </a>
            <a href="/contact" class="inline-flex items-center gap-2 bg-accent text-neutral-dark font-semibold px-4 py-3 rounded-xl shadow-lg hover:bg-[var(--color-accent-strong)] transition">
              Get a quote
              <span aria-hidden="true">→</span>
            </a>
          </div>

          <details class="md:hidden relative">
            <summary class="flex items-center gap-3 text-neutral-white cursor-pointer select-none border border-white/15 rounded-xl px-3 py-2 hover:border-white/30 transition">
              <span class="font-semibold">Menu</span>
              <svg class="h-5 w-5" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.8">
                <path stroke-linecap="round" d="M3 6h14M3 10h14M3 14h14" />
              </svg>
            </summary>
            <div class="absolute right-0 mt-3 w-64 bg-neutral-dark border border-white/15 rounded-2xl shadow-2xl p-4 flex flex-col gap-3 text-neutral-light-gray">
              {navLinks.map((link) => (
                <a
                  href={link.href}
                  aria-current={isActive(link.href) ? "page" : undefined}
                  class={`hover:text-accent transition ${isActive(link.href) ? "text-accent font-semibold" : ""}`}
                >
                  {link.label}
                </a>
              ))}
              <a href="/contact" class="inline-flex justify-center items-center gap-2 bg-accent text-neutral-dark font-semibold px-3 py-3 rounded-xl shadow hover:bg-[var(--color-accent-strong)] transition">
                Get a quote
              </a>
            </div>
          </details>
        </div>
      </div>
    </nav>

    <slot />

    <footer class="bg-neutral-dark text-neutral-light mt-20">
      <div class="max-w-7xl mx-auto px-6 py-14 grid md:grid-cols-4 gap-10">
        <div class="md:col-span-2 space-y-3">
          <div class="flex items-center gap-3">
            <img src={logo.src} alt="Luna Handyman Solutions logo" class="h-11 w-11 rounded-xl shadow-lg" />
            <div>
              <p class="text-lg font-bold text-neutral-white">Luna Handyman Solutions</p>
              <p class="text-sm text-neutral-light-gray">Craftsmanship, clarity, and jobsite care on every build.</p>
            </div>
          </div>
          <p class="text-neutral-light-gray">
            Full-service construction and renovation crews serving residential and commercial clients with transparent communication and reliable delivery.
          </p>
          <div class="flex items-center gap-3">
            <a href="/contact" class="inline-flex items-center gap-2 bg-accent text-neutral-dark font-semibold px-4 py-3 rounded-xl shadow hover:bg-[var(--color-accent-strong)] transition">
              Book a walkthrough
            </a>
            <a href="tel:+14157678986" class="inline-flex items-center gap-2 text-neutral-light-gray hover:text-accent transition">
              <span class="text-lg">📞</span> 415-767-8986
            </a>
          </div>
        </div>

        <div class="space-y-3">
          <p class="text-sm uppercase tracking-[0.18em] text-neutral-light-gray">Company</p>
          <div class="flex flex-col gap-2 text-neutral-white">
            <a href="/about" class="hover:text-accent transition">About</a>
            <a href="/services" class="hover:text-accent transition">Services</a>
            <a href="/projects" class="hover:text-accent transition">Projects</a>
            <a href="/contact" class="hover:text-accent transition">Contact</a>
          </div>
        </div>

        <div class="space-y-3">
          <p class="text-sm uppercase tracking-[0.18em] text-neutral-light-gray">Service area</p>
          <p class="text-neutral-white leading-relaxed">
            Bay Area, California<br />
            On-site by appointment
          </p>
          <p class="text-neutral-light-gray">Mon - Sat · 7am - 6pm</p>
        </div>
      </div>
      <div class="border-t border-white/10 text-center text-sm text-neutral-light-gray py-5">
        © {new Date().getFullYear()} Luna Handyman Solutions. All rights reserved.
      </div>
    </footer>
  </body>
</html>

<style>
  nav summary::-webkit-details-marker {
    display: none;
  }
</style>
