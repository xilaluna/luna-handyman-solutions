---
import BaseLayout from '../layouts/BaseLayout.astro';
import { projects } from '../data/projects';
---

<BaseLayout title="Our Projects | Luna Handyman Solutions">
  <section class="relative isolate overflow-hidden bg-gradient-to-br from-neutral-dark via-neutral-dark to-secondary">
    <div class="absolute -left-24 top-10 h-64 w-64 rounded-full bg-accent/30 blur-3xl"></div>
    <div class="absolute right-0 bottom-8 h-72 w-72 rounded-full bg-primary/25 blur-3xl"></div>

    <div class="relative max-w-6xl mx-auto px-6 py-20 md:py-24 text-neutral-white">
      <div class="inline-flex items-center gap-3 bg-white/10 border border-white/20 rounded-full px-4 py-2 backdrop-blur">
        <span class="h-2 w-2 rounded-full bg-accent animate-pulse"></span>
        <p class="text-sm font-semibold uppercase tracking-[0.2em]">Project portfolio</p>
      </div>
      <div class="grid md:grid-cols-[1.2fr,0.8fr] gap-10 items-center mt-10">
        <div class="space-y-4">
          <h1 class="text-4xl md:text-5xl font-bold leading-tight drop-shadow-lg">Builds that stay on schedule and on spec.</h1>
          <p class="text-lg text-neutral-light-gray leading-relaxed max-w-2xl">
            A selection of renovations, tenant improvements, and ground-up builds delivered with disciplined sequencing, vetted trades, and finish-grade supervision.
          </p>
          <div class="flex flex-wrap gap-3">
            <span class="px-4 py-2 rounded-full border border-white/25 bg-white/10 text-sm font-semibold">Ground-up</span>
            <span class="px-4 py-2 rounded-full border border-white/25 bg-white/10 text-sm font-semibold">Renovations</span>
            <span class="px-4 py-2 rounded-full border border-white/25 bg-white/10 text-sm font-semibold">Tenant improvements</span>
          </div>
        </div>

        <div class="bg-white/10 backdrop-blur-xl border border-white/15 rounded-3xl shadow-2xl p-6 space-y-4">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm uppercase tracking-[0.16em] text-neutral-light-gray">Snapshot</p>
              <p class="text-3xl font-bold text-neutral-white">Built on trust</p>
            </div>
            <span class="inline-flex items-center gap-2 bg-accent text-neutral-dark px-3 py-1 rounded-full text-sm font-semibold shadow">
              4.9★ rating
            </span>
          </div>
          <div class="grid grid-cols-3 gap-3 text-center">
            <div class="rounded-2xl bg-white/10 border border-white/10 px-3 py-4">
              <p class="text-2xl font-bold text-accent">100+</p>
              <p class="text-xs uppercase tracking-wide text-neutral-light-gray">Projects</p>
            </div>
            <div class="rounded-2xl bg-white/10 border border-white/10 px-3 py-4">
              <p class="text-2xl font-bold text-neutral-white">15+</p>
              <p class="text-xs uppercase tracking-wide text-neutral-light-gray">Years</p>
            </div>
            <div class="rounded-2xl bg-white/10 border border-white/10 px-3 py-4">
              <p class="text-2xl font-bold text-primary">Zero</p>
              <p class="text-xs uppercase tracking-wide text-neutral-light-gray">Tolerance for rework</p>
            </div>
          </div>
          <ul class="space-y-2 text-neutral-light-gray">
            <li class="flex items-start gap-3"><span class="mt-1 h-2 w-2 rounded-full bg-accent"></span>Dedicated site leads and milestone reporting on every job.</li>
            <li class="flex items-start gap-3"><span class="mt-1 h-2 w-2 rounded-full bg-accent"></span>Phased approaches for occupied spaces and business continuity.</li>
            <li class="flex items-start gap-3"><span class="mt-1 h-2 w-2 rounded-full bg-accent"></span>Quality checks and punch-list ownership until closeout.</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <main class="space-y-20 py-16">
    <section class="max-w-7xl mx-auto px-6">
      <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-6 mb-10">
        <div class="space-y-2">
          <p class="text-sm font-semibold uppercase tracking-[0.2em] text-primary">Recent builds</p>
          <h2 class="text-4xl font-bold text-secondary leading-tight">A closer look at our work.</h2>
          <p class="text-neutral-dark-gray text-lg max-w-3xl">
            Projects spanning residential, commercial, and specialty scopes—all delivered with predictable schedules, safety-first crews, and finish-grade attention.
          </p>
        </div>
        <a href="/contact" class="inline-flex items-center gap-2 bg-neutral-dark text-neutral-white px-5 py-3 rounded-xl font-semibold shadow-lg hover:bg-black transition">
          Start a project consult
          <span aria-hidden="true" class="text-lg">→</span>
        </a>
      </div>

      <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
        {projects.map(project => (
          <div class="group bg-white rounded-3xl shadow-lg border border-neutral-200 overflow-hidden transition hover:-translate-y-2 hover:shadow-2xl">
            <div class="relative h-52 overflow-hidden">
              <img
                src={project.image}
                alt={project.title}
                class="w-full h-full object-cover transition duration-500 group-hover:scale-105"
                loading="lazy"
              />
              <div class="absolute inset-x-0 bottom-0 p-4 flex items-center justify-between bg-gradient-to-t from-neutral-dark/80 via-neutral-dark/40 to-transparent text-neutral-white">
                <span class="inline-flex items-center gap-2 bg-accent text-neutral-dark px-3 py-1 rounded-full text-xs font-semibold shadow">
                  {project.status}
                </span>
                <p class="text-sm font-semibold">{project.timeline}</p>
              </div>
            </div>

            <div class="p-5 space-y-4">
              <div class="flex items-center justify-between">
                <p class="text-sm uppercase tracking-[0.16em] text-primary font-semibold">{project.scope}</p>
                <span class="inline-flex items-center gap-1 text-sm text-neutral-dark-gray">
                  <span class="text-lg">📍</span> {project.location}
                </span>
              </div>
              <h3 class="text-2xl font-bold text-neutral-dark">{project.title}</h3>
              <p class="text-neutral-dark-gray leading-relaxed text-sm">{project.description}</p>
              <div class="flex items-center gap-3 text-sm font-semibold">
                <a href="/contact" class="inline-flex items-center gap-2 text-primary hover:text-secondary transition">
                  Discuss a similar build
                  <span aria-hidden="true">→</span>
                </a>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    <section class="relative mx-6 overflow-hidden rounded-3xl">
      <div class="absolute inset-0 bg-gradient-to-r from-secondary via-neutral-dark to-secondary"></div>
      <div class="absolute -left-20 -bottom-10 h-52 w-52 rounded-full bg-accent/30 blur-3xl"></div>
      <div class="absolute right-6 top-6 h-20 w-20 rounded-full border border-white/30"></div>
      <div class="absolute right-20 top-16 h-10 w-10 rounded-full border border-white/20"></div>

      <div class="relative max-w-6xl mx-auto px-8 py-16 md:py-20 grid md:grid-cols-[1.2fr,0.8fr] gap-10 items-center text-neutral-white">
        <div class="space-y-4">
          <p class="text-sm uppercase tracking-[0.18em] text-accent font-semibold">Let’s plan yours</p>
          <h2 class="text-4xl md:text-5xl font-bold leading-tight">Tell us your scope—we’ll map the build path.</h2>
          <p class="text-lg text-neutral-light-gray leading-relaxed max-w-3xl">
            Share plans or ideas and we’ll return a clear scope, timeline, and budget with a dedicated site lead before mobilization.
          </p>
          <div class="flex flex-col sm:flex-row gap-3">
            <a href="/contact" class="inline-flex items-center justify-center gap-2 bg-accent text-neutral-dark font-bold py-4 px-6 rounded-xl shadow-lg hover:bg-[var(--color-accent-strong)] transition">
              Book a walkthrough
              <span aria-hidden="true" class="text-lg">→</span>
            </a>
            <a href="/services" class="inline-flex items-center justify-center gap-2 border border-white/30 text-neutral-white font-semibold py-4 px-6 rounded-xl hover:border-white hover:bg-white/10 transition">
              Explore services
            </a>
          </div>
        </div>

        <div class="bg-white/10 backdrop-blur-xl rounded-2xl border border-white/15 shadow-2xl p-6 md:p-7 space-y-4">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm uppercase tracking-[0.14em] text-neutral-light-gray">Project kickoff</p>
              <p class="text-xl font-bold text-neutral-white mt-1">What to expect</p>
            </div>
            <span class="inline-flex items-center gap-2 bg-accent text-neutral-dark px-3 py-1 rounded-full text-sm font-semibold">
              48h response
            </span>
          </div>

          <div class="space-y-3 text-neutral-light-gray">
            <p class="flex items-start gap-3"><span class="mt-1 h-2 w-2 rounded-full bg-accent"></span>Dedicated site lead assigned early, with daily updates.</p>
            <p class="flex items-start gap-3"><span class="mt-1 h-2 w-2 rounded-full bg-accent"></span>Clear schedule, procurement plan, and safety protocols.</p>
            <p class="flex items-start gap-3"><span class="mt-1 h-2 w-2 rounded-full bg-accent"></span>Weekly photo reports and budget tracking for stakeholders.</p>
          </div>

          <div class="grid grid-cols-2 gap-3">
            <div class="rounded-xl bg-white/5 border border-white/10 px-4 py-4">
              <p class="text-2xl font-bold text-neutral-white">Site</p>
              <p class="text-sm text-neutral-light-gray">On-site in 72 hours</p>
            </div>
            <div class="rounded-xl bg-white/5 border border-white/10 px-4 py-4">
              <p class="text-2xl font-bold text-neutral-white">Docs</p>
              <p class="text-sm text-neutral-light-gray">Permits & inspections handled</p>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
</BaseLayout>
