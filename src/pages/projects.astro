---
import BaseLayout from "../layouts/BaseLayout.astro";
import { projects } from "../data/projects";
import { Image } from "astro:assets";

---

<BaseLayout title="Projects | Luna Handyman Solutions">
  <section class="relative overflow-hidden bg-gradient-to-br from-neutral-white via-neutral-light to-white section-padding">
    <div class="absolute -left-24 top-8 h-64 w-64 rounded-full bg-primary/10 blur-3xl"></div>
    <div class="absolute right-0 bottom-10 h-64 w-64 rounded-full bg-accent/20 blur-3xl"></div>

    <div class="relative section-container space-y-6">
      <div class="space-y-3 max-w-4xl">
        <p class="text-sm uppercase tracking-[0.2em] text-primary font-semibold">Project portfolio</p>
        <h1 class="display-font text-4xl md:text-5xl font-semibold text-secondary leading-[1.05]">Builds that stay on schedule and on spec.</h1>
        <p class="text-lg text-neutral-dark-gray leading-relaxed max-w-3xl">
          Renovations, tenant improvements, and structural upgrades delivered with tidy protection, coordinated trades, and daily updates you can act on.
        </p>
      </div>

      <div class="flex flex-wrap gap-2">
        <span class="inline-flex items-center gap-2 rounded-full bg-neutral-light px-3 py-1 text-sm text-neutral-dark"><span class="h-2 w-2 rounded-full bg-primary"></span>Renovations</span>
        <span class="inline-flex items-center gap-2 rounded-full bg-neutral-light px-3 py-1 text-sm text-neutral-dark"><span class="h-2 w-2 rounded-full bg-primary"></span>Tenant improvements</span>
        <span class="inline-flex items-center gap-2 rounded-full bg-neutral-light px-3 py-1 text-sm text-neutral-dark"><span class="h-2 w-2 rounded-full bg-primary"></span>Structural & exterior</span>
      </div>
    </div>
  </section>

  <main class="space-y-20 md:space-y-24">
    <section class="section-container section-padding">
      <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-6 mb-10">
        <div class="space-y-2">
          <p class="text-sm font-semibold uppercase tracking-[0.2em] text-primary">Recent builds</p>
          <h2 class="display-font text-3xl md:text-4xl font-semibold text-secondary leading-tight">A closer look at our work.</h2>
          <p class="text-neutral-dark-gray text-lg max-w-3xl">
            Proof across residential, commercial, and specialty scopes—each run with one accountable lead, vetted trades, and finish-grade supervision.
          </p>
        </div>
        <a
          href="/contact"
          class="inline-flex items-center gap-2 bg-primary text-neutral-white px-5 py-3 rounded-xl font-semibold shadow-md hover:bg-primary-dark transition"
        >
          Start a consult
          <span aria-hidden="true" class="text-lg">→</span>
        </a>
      </div>

      <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
        {projects.map((project) => (
          <div class="group bg-white rounded-3xl shadow-lg border border-neutral-200 overflow-hidden transition hover:-translate-y-2 hover:shadow-2xl">
            <div class="relative h-52 overflow-hidden bg-neutral-light">
              <Image
                src={project.thumbnail} 
                alt={project.title}
                class="w-full h-full object-cover transition duration-500 group-hover:scale-105"
                loading="lazy"
              />
              <div class="absolute inset-x-0 bottom-0 p-4 flex items-center justify-between bg-gradient-to-t from-neutral-dark/80 via-neutral-dark/30 to-transparent text-neutral-white">
                <span class="inline-flex items-center gap-2 bg-accent text-neutral-dark px-3 py-1 rounded-full text-xs font-semibold shadow">
                  {project.status}
                </span>
                <p class="text-sm font-semibold">{project.timeline}</p>
              </div>
            </div>

            <div class="p-5 space-y-3">
              <div class="flex items-center justify-between">
                <p class="text-sm uppercase tracking-[0.16em] text-primary font-semibold">{project.scope}</p>
                <span class="inline-flex items-center gap-1 text-sm text-neutral-dark-gray">
                  <span class="text-lg">📍</span> {project.location}
                </span>
              </div>
              <h3 class="display-font text-xl font-semibold text-secondary">{project.title}</h3>
              <p class="text-neutral-dark-gray leading-relaxed text-sm">{project.description}</p>
              <div class="flex items-center gap-3 text-sm font-semibold">
                <a href="/contact" class="inline-flex items-center gap-2 text-primary hover:text-secondary transition">
                  Discuss a similar build
                  <span aria-hidden="true">→</span>
                </a>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    <section class="bg-secondary text-neutral-light section-padding">
      <div class="section-container space-y-8">
        <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
          <div class="space-y-2">
            <p class="text-sm uppercase tracking-[0.2em] text-accent font-semibold">Process</p>
            <h3 class="display-font text-3xl md:text-4xl font-semibold text-neutral-white leading-tight">Same rhythm on every project.</h3>
            <p class="text-neutral-light-gray text-lg max-w-2xl">Scope, plan, build, close—kept predictable with daily updates, tidy protection, and one accountable lead.</p>
          </div>
          <a
            href="/contact"
            class="inline-flex items-center justify-center gap-2 rounded-xl border border-white/30 px-5 py-3 text-neutral-white font-semibold hover:bg-white/10 transition"
          >
            Schedule a walkthrough
          </a>
        </div>

        <div class="grid md:grid-cols-4 gap-4">
          <div class="rounded-2xl bg-neutral-white/5 border border-white/10 p-5">
            <div class="h-10 w-10 rounded-full bg-primary/20 text-neutral-white font-semibold flex items-center justify-center mb-3">01</div>
            <p class="display-font text-lg font-semibold text-neutral-white mb-1">Scope & site walk</p>
            <p class="text-neutral-light-gray text-sm leading-relaxed">Document conditions, risks, and constraints—align budget and milestones.</p>
          </div>
          <div class="rounded-2xl bg-neutral-white/5 border border-white/10 p-5">
            <div class="h-10 w-10 rounded-full bg-accent/30 text-neutral-dark font-semibold flex items-center justify-center mb-3">02</div>
            <p class="display-font text-lg font-semibold text-neutral-white mb-1">Plan & permit</p>
            <p class="text-neutral-light-gray text-sm leading-relaxed">Schedule, procurement, permitting, and inspections handled before mobilization.</p>
          </div>
          <div class="rounded-2xl bg-neutral-white/5 border border-white/10 p-5">
            <div class="h-10 w-10 rounded-full bg-neutral-light/20 text-neutral-white font-semibold flex items-center justify-center mb-3">03</div>
            <p class="display-font text-lg font-semibold text-neutral-white mb-1">Build + update</p>
            <p class="text-neutral-light-gray text-sm leading-relaxed">Coordinated trades, daily logs, and clean protection for occupied spaces.</p>
          </div>
          <div class="rounded-2xl bg-neutral-white/5 border border-white/10 p-5">
            <div class="h-10 w-10 rounded-full bg-primary/20 text-neutral-white font-semibold flex items-center justify-center mb-3">04</div>
            <p class="display-font text-lg font-semibold text-neutral-white mb-1">QC & handoff</p>
            <p class="text-neutral-light-gray text-sm leading-relaxed">Punch resolved quickly, warranties issued, and support if you need it.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="section-padding px-6">
      <div class="max-w-6xl mx-auto bg-white border border-neutral-light-gray rounded-3xl shadow-lg px-6 md:px-10 py-10 flex flex-col md:flex-row md:items-center md:justify-between gap-6">
        <div class="space-y-2">
          <p class="text-sm font-semibold uppercase tracking-[0.2em] text-primary">Ready to start?</p>
          <h4 class="display-font text-2xl md:text-3xl font-semibold text-secondary">Let’s map your scope and timeline.</h4>
          <p class="text-neutral-dark-gray text-lg leading-relaxed max-w-2xl">
            Share your plans or ideas and we’ll return a clear budget, schedule, and next steps.
          </p>
        </div>
        <div class="flex flex-col sm:flex-row gap-3 w-full md:w-auto">
          <a
            href="/contact"
            class="inline-flex items-center justify-center gap-2 bg-primary text-neutral-white font-semibold px-5 py-3 rounded-xl shadow-md hover:bg-primary-dark transition"
          >
            Book a consult
            <span aria-hidden="true">→</span>
          </a>
          <a
            href="tel:+14157678986"
            class="inline-flex items-center justify-center gap-2 rounded-xl border border-secondary/20 px-5 py-3 text-secondary font-semibold hover:border-secondary/50 transition"
          >
            Call 415-767-8986
          </a>
        </div>
      </div>
    </section>
  </main>
</BaseLayout>
